class TopDestinationModel {
  String image, cityName, name, address, days;
  TopDestinationModel({
    required this.image,
    required this.cityName,
    required this.name,
    required this.address,
    required this.days,
  });
}
